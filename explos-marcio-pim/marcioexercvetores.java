public class marcioexercvetores{
    public static void main (String[] args) {
        int[] vetor = new int[6];
        vetor[0] = 1;
        vetor[1] = 2;
        int soma = A[0] + A[1] + A[5];
        System.out.println(" A Soma dos trés valores é "+ soma);
        A[4] = 100;
        System.out.println("Impressão do vetor 1 em cada linha");
        for (int i = 0; i < v.lengt; i += 1) {
            System.out.println(A[i]);

            int[] v = new int[6];
                for (int i = 0; )
        }
    }
}